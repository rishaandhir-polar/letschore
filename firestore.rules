rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DIAGNOSTIC MODE: Open to all while we find the port conflict
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
